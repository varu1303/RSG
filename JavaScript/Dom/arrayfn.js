// console.log('MAIN')

{
    let a = [1, 2, 3];

    // forEach, map, filter

    // forEach
    // let fn1 = (item, index) => {
    //     console.log(item, index);
    //         return item * 2;
    // };
    // let b;
    // b = a.forEach(fn1)

    // map, filter | returns a new array

    // let b = a.map((item, index) => {
    //     console.log(item, index);
    //     return item * 2;
    // })

    // console.log(a);
    // console.log(b);

    // filter

    let b;
    // b = a.filter((item, index) => {
    //     if (item > 2)
    //         return true;
    //     else
    //         return false;
    // })

    // b = a.filter((item, index) => {
    //     return item > 2;
    // }).map((item) => {
    //     return item * 2;
    // })
    // b.push('10');

    console.log(a);
    console.log(b);
}
